@{
    ViewData["Title"] = "Projects";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@foreach(var project in Model)
    {
        @if(project.stargazer_count >= 1)
        {

        <h2>@project.name</h2>
        <a href="@project.html_url">GitHub Link To Project</a>

        }
    }